# ╔══════════════════════════════════════════════════════════════╗
# ║        Configuración para Laptop / Notebook (Sway)           ║
# ║        Touchpad, Gestos, Brillo, Batería, Tapa               ║
# ╚══════════════════════════════════════════════════════════════╝

# ── Touchpad ───────────────────────────────────────────────────
input type:touchpad {
    dwt enabled
    tap enabled
    natural_scroll enabled
    middle_emulation enabled
    scroll_factor 0.4
}

# ── Gestos Táctiles Touchpad (Swipe) ────────────────────────────
bindgesture swipe:3:right workspace next
bindgesture swipe:3:left workspace prev
bindgesture swipe:4:up floating toggle
bindgesture swipe:4:down focus mode_toggle

# ── Control de Brillo (Pantalla Integrada) ──────────────────────
bindsym --locked XF86MonBrightnessDown exec $scripts/brightness.sh down
bindsym --locked XF86MonBrightnessUp exec $scripts/brightness.sh up

# ── Comportamiento de Tapa (Lid Switch) ─────────────────────────
bindswitch --reload --locked lid:on exec $lock

# ── Monitoreo de Batería Baja ──────────────────────────────────
exec $scripts/battery-alert.sh
